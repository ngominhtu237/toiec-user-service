function fields (query) {
  if (!query.fields) return null;
  return query.fields.replace(/,/g, ' ');
}

module.exports = function () {
  return function (req, res, next) {
    req.fields = fields(req.query);
    next();
  };
};
